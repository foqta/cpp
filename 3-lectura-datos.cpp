#include <iostream>

using namespace std;

int main() {
    int numero; // Declaramos la variable numero
    cout << "Ingrese un numero entero: "; // Preguntamos al usuario por un numero
    cin >> numero; // Leemos el numero ingresado por el usuario
    cout << "El numero ingresado es: " << numero << endl; // Mostramos el numero al usuario
    system("pause"); // Pausamos la ejecución del programa para que el usuario pueda ver el resultado
    return 0;
}

// compilado correcto