#include <iostream>

using namespace std;

int main() {

    float a, b, res; // Variables para almacenar los valores de entrada
    cout << "Rellene los valores de a y b:\n "; // Esto es para que pueda ingresar los valores en una sola línea
    cout << "a: "; // Esto es para que pueda ingresar los valores en una sola línea
    cin >> a; // Leemos el primer número
    cout << "\n b: "; // Leemos el segundo número
    cin >> b; // Leemos el segundo número
    res = (a / b) + 1 ; // Sumamos los valores de entrada
    cout << "\nEl resultado es: " << res; // Mostramos el resultado






    system("pause");
    return 0;
}

